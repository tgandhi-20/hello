// PLACEHOLDER — owned by Agent 5 (Insight). Replace with the dashboard:
// Safe-to-Spend hero, month spend vs budget, category donut, sparkline, recent txns.
export function HomeScreen() {
  return (
    <div className="px-4 py-6">
      <p className="text-sm text-text-2">Home / Dashboard — Agent 5 builds this screen.</p>
    </div>
  );
}
