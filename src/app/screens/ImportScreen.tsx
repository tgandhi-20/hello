// PLACEHOLDER — owned by Agent 3 (Import). Replace with CSV upload, bank auto-detection
// (CBA/Bankwest/Amex), and the preview-and-confirm screen before committing an import.
export function ImportScreen() {
  return (
    <div className="px-4 py-6">
      <p className="text-sm text-text-2">Import — Agent 3 builds this screen.</p>
    </div>
  );
}
