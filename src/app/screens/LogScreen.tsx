// PLACEHOLDER — owned by Agent 4 (Logging). Replace with quick-add: category tile grid,
// custom numeric keypad, one-tap save, haptic confirm, undo toast.
export function LogScreen() {
  return (
    <div className="px-4 py-6">
      <p className="text-sm text-text-2">Quick-add — Agent 4 builds this screen.</p>
    </div>
  );
}
