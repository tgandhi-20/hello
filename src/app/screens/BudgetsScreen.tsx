// PLACEHOLDER — owned by Agent 5 (Insight). Replace with per-category monthly budgets,
// progress vs limitCents, and the budget-editing flow.
export function BudgetsScreen() {
  return (
    <div className="px-4 py-6">
      <p className="text-sm text-text-2">Budgets — Agent 5 builds this screen.</p>
    </div>
  );
}
