# Debug builds are what CI produces for sideloading; release minification is
# disabled for now (see app/build.gradle.kts). This file is a placeholder so
# the proguardFiles() reference in build.gradle.kts resolves cleanly if/when
# a real release build is ever configured.
